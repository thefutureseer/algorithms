//get the sum of all hourglass integers in the given 2D array

function hourglassSum(arr) {
  //variable set to the max any hourglass can possibly be (-9x7)
  let max = -68;

  //two for loops to handle a 2D array and only within the 
  //boundries of the
  for (var i = 0; i < 4; i++) {
    for (var i = 0; i < 4; i++) {
      let sum = 
    }
  }
}